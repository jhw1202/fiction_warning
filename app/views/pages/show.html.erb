<div class="title">
  <h1><%= @title %></h1>
</div>

  <div class="content">
    <%= @page.content %>
  </div>

<div class="choice-list">
  <% @choices.each do |choice| %>
    <div class="choice">
      <%= link_to choice.title, "#{choice.child_id}" %>
    </div>
  <% end %>
</div>
